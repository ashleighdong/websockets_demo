PORT=3000
# Do not use the URI below as-is — cluster.example.mongodb.net is a fake host. In Atlas: Database → Connect → Drivers, copy your real string into .env.
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.example.mongodb.net/websockets_demo?retryWrites=true&w=majority
